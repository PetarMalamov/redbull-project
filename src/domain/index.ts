export type Driver = {
  uuid: string;
  firstName: string;
  lastName: string;
  constructorName: string;
  pointsTotal: number;
};

export type Post = {
  title: string;
  type: string;
  readTime: string;
  image: string;
};
